## 共通

- 思考は英語で、回答は日本語で
- ファイル名やフォルダ名は特に縛りがない限りケバブケースを採用する
- コードコメントは日本語で記述すること

## Node.js

- `pnpm`コマンドを優先して利用すること

## TypeScript

- エラークラスの定義以外では`class`の利用は禁止
- `any`型の利用は禁止。代わりに`unknown`と型ガードの組み合わせで型情報を処理すること
- `as any`や`as unknown as any`のようなキャストは禁止

